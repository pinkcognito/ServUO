
# Forces scripts to be compiled at runtime.
# This is a legacy feature that was originally provided by the core.
#
# Disabled for deployed instances: the deploy pipeline already builds
# Scripts.dll in Release mode as part of CI, and the deploy artifact
# intentionally doesn't ship Scripts/ source or a .NET SDK to the instance -
# dynamic compilation would just fail looking for a directory that was
# never deployed.
Dynamic=False